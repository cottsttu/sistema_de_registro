diff --git a/agentes.html b/agentes.html
index 2e233d3..bf5e62c 100644
--- a/agentes.html
+++ b/agentes.html
@@ -7,7 +7,7 @@
     <link rel="preload" as="image" href="src/imagem_fundo_branco.png" fetchpriority="high">
     <link rel="preload" as="image" href="src/imagem_de_fundo_login_editada.jpeg">
     <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
-    <link rel="stylesheet" href="css/agentes.css?v=20260626-1">
+    <link rel="stylesheet" href="css/agentes.css?v=20260702-2">
     <link rel="stylesheet" href="css/app-menu.css?v=20260626-1">
 </head>
 <body data-theme="day">
@@ -223,7 +223,7 @@
 <script src="js/agentes.js"></script>
 
 
-<script src="js/agentes-2.js?v=20260701-2"></script>
+<script src="js/agentes-2.js?v=20260702-2"></script>
 
 </body>
 </html>
diff --git a/css/agentes.css b/css/agentes.css
index 26214aa..9f80ea4 100644
--- a/css/agentes.css
+++ b/css/agentes.css
@@ -1377,6 +1377,114 @@ body[data-theme="night"] .modal-detalhes-item strong {
     cursor: pointer;
 }
 
+.btn-disponibilidade-smartwall {
+    min-width: 94px;
+    min-height: 30px;
+    padding: 6px 10px !important;
+    border-radius: 6px !important;
+    font-size: 10px !important;
+    font-weight: 900 !important;
+    line-height: 1 !important;
+}
+
+.btn-disponibilidade-smartwall[data-disponivel="true"] {
+    color: #ffffff !important;
+    border-color: rgba(34, 197, 94, 0.55) !important;
+    background: #1f9d55 !important;
+}
+
+.btn-disponibilidade-smartwall[data-disponivel="false"] {
+    color: #ffffff !important;
+    border-color: rgba(108, 117, 125, 0.55) !important;
+    background: #6c757d !important;
+}
+
+.modal-motivo-indisponibilidade {
+    position: fixed;
+    inset: 0;
+    z-index: 2500;
+    display: none;
+    align-items: center;
+    justify-content: center;
+    padding: 16px;
+}
+
+.modal-motivo-indisponibilidade.open {
+    display: flex;
+}
+
+.modal-motivo-backdrop {
+    position: absolute;
+    inset: 0;
+    background: rgba(8, 18, 24, 0.68);
+}
+
+.modal-motivo-card {
+    position: relative;
+    z-index: 1;
+    width: min(420px, 100%);
+    display: grid;
+    gap: 12px;
+    padding: 16px;
+    border-radius: 10px;
+    color: #1d2d36;
+    background: #ffffff;
+    box-shadow: 0 18px 44px rgba(0, 0, 0, 0.34);
+}
+
+body[data-theme="night"] .modal-motivo-card {
+    color: #f5fbff;
+    background: #102431;
+}
+
+.modal-motivo-card h2 {
+    margin: 0;
+    font-size: 15px;
+    font-weight: 900;
+    text-transform: uppercase;
+}
+
+.modal-motivo-card label {
+    display: grid;
+    gap: 6px;
+    font-size: 11px;
+    font-weight: 800;
+    text-transform: uppercase;
+}
+
+.modal-motivo-card textarea {
+    width: 100% !important;
+    min-height: 92px;
+    resize: vertical;
+    text-transform: uppercase;
+}
+
+.modal-motivo-actions {
+    display: flex;
+    justify-content: flex-end;
+    gap: 8px;
+}
+
+.modal-motivo-actions button {
+    min-height: 34px;
+    border: 0;
+    border-radius: 7px;
+    padding: 0 14px;
+    color: #ffffff;
+    font-size: 11px;
+    font-weight: 900;
+    text-transform: uppercase;
+    cursor: pointer;
+}
+
+.btn-motivo-cancelar {
+    background: #6c757d;
+}
+
+.btn-motivo-confirmar {
+    background: #1f9d55;
+}
+
 #relatorioFinal .acao-botoes {
     display: flex;
     align-items: center;
diff --git a/css/smartwall.css b/css/smartwall.css
index f86937c..00a6200 100644
--- a/css/smartwall.css
+++ b/css/smartwall.css
@@ -12,7 +12,6 @@
     --sw-red: #ff4c5d;
     --sw-shadow: 0 22px 60px rgba(0, 0, 0, 0.38);
 }
-
 * {
     box-sizing: border-box;
 }
@@ -250,23 +249,26 @@ body[data-theme="night"] .smart-theme-switch.theme-switch::after {
     margin: 0 auto;
     display: grid;
     grid-template-columns: repeat(5, minmax(165px, 1fr));
-    gap: 12px;
+    gap: 10px;
 }
 
 .kpi-card {
-    min-height: 104px;
+    min-height: 68px;
     position: relative;
     display: grid;
-    grid-template-columns: 58px minmax(0, 1fr);
+    grid-template-columns: 42px minmax(0, 1fr);
     align-items: center;
-    gap: 12px;
-    padding: 14px 16px;
-    border-radius: 14px;
+    gap: 8px;
+    padding: 7px 12px;
+    border-radius: 10px;
     overflow: hidden;
 }
 
 .kpi-card > div:last-child {
     min-width: 0;
+    display: grid;
+    grid-template-rows: minmax(2.15rem, auto) minmax(1.45rem, auto);
+    align-items: center;
     text-align: center;
 }
 
@@ -276,32 +278,32 @@ body[data-theme="night"] .smart-theme-switch.theme-switch::after {
     left: 0;
     right: 0;
     bottom: 0;
-    height: 4px;
+    height: 3px;
     background: var(--accent-color);
     box-shadow: 0 0 22px var(--accent-color);
 }
 
 .stat-icon {
-    width: 58px;
-    height: 58px;
+    width: 42px;
+    height: 42px;
     display: flex;
     align-items: center;
     justify-content: center;
     border: 1px solid color-mix(in srgb, var(--accent-color) 70%, transparent);
-    border-radius: 13px;
+    border-radius: 10px;
     color: var(--accent-color);
     background: color-mix(in srgb, var(--accent-color) 15%, transparent);
     box-shadow: inset 0 0 24px color-mix(in srgb, var(--accent-color) 20%, transparent);
 }
 
 .stat-icon svg {
-    width: 31px;
-    height: 31px;
+    width: 24px;
+    height: 24px;
 }
 
 .stat-icon img {
-    width: 34px;
-    height: 34px;
+    width: 25px;
+    height: 25px;
     object-fit: contain;
     filter: drop-shadow(0 0 8px color-mix(in srgb, var(--accent-color) 40%, transparent));
 }
@@ -315,41 +317,46 @@ body[data-theme="night"] .kpi-card .stat-icon img {
 }
 
 .kpi-card h3 {
-    margin: 0;
-    font-size: clamp(0.78rem, 0.78vw, 0.98rem);
+    min-height: 1.45rem;
+    margin: 1px 0 0;
+    color: var(--sw-text);
+    font-size: clamp(0.58rem, 0.6vw, 0.74rem);
     font-weight: 900;
+    line-height: 1.08;
     text-transform: uppercase;
     letter-spacing: 0.02em;
 }
 
 .kpi-card .value {
-    margin-top: 5px;
+    min-height: 2.15rem;
     color: var(--accent-color);
-    font-size: clamp(1.85rem, 2.5vw, 3.2rem);
+    font-size: clamp(1.45rem, 1.9vw, 2.35rem);
     line-height: 0.95;
     font-weight: 950;
     display: flex;
     align-items: center;
     justify-content: center;
+    flex-wrap: nowrap;
+    white-space: nowrap;
 }
 
 .fleet-value {
     display: inline-flex;
     align-items: center;
-    gap: 10px;
-    font-size: clamp(1.55rem, 2.2vw, 2.9rem);
+    gap: 7px;
+    font-size: clamp(1.32rem, 1.8vw, 2.25rem);
     white-space: nowrap;
 }
 
 .fleet-value + .fleet-value {
-    margin-left: 18px;
-    padding-left: 18px;
+    margin-left: 12px;
+    padding-left: 12px;
     border-left: 1px solid rgba(255, 255, 255, 0.35);
 }
 
 .fleet-icon {
-    width: 36px;
-    height: 36px;
+    width: 28px;
+    height: 28px;
     object-fit: contain;
     filter: drop-shadow(0 0 8px rgba(255, 255, 255, 0.16));
 }
@@ -368,7 +375,7 @@ body[data-theme="night"] .fleet-icon {
 
 .fleet-count {
     color: var(--accent-color);
-    font-size: 37px;
+    font-size: 30px;
     font-weight: 950;
 }
 
@@ -376,12 +383,6 @@ body[data-theme="night"] .fleet-icon {
     margin-right: 0;
 }
 
-.kpi-card p {
-    margin: 6px 0 0;
-    color: var(--sw-text);
-    font-size: 0.78rem;
-}
-
 .border-blue,
 .value-blue {
     --accent-color: var(--sw-blue);
@@ -405,7 +406,7 @@ body[data-theme="night"] .fleet-icon {
 .main-content {
     min-height: 0;
     display: grid;
-    grid-template-columns: minmax(560px, 1.62fr) minmax(300px, 0.88fr) minmax(300px, 0.82fr);
+    grid-template-columns: minmax(560px, 1.82fr) minmax(260px, 0.68fr) minmax(285px, 0.72fr);
     gap: 12px;
     overflow: hidden;
 }
@@ -458,7 +459,7 @@ body[data-theme="night"] .fleet-icon {
 
 .region-panel {
     display: grid;
-    grid-template-columns: minmax(315px, 1fr) minmax(265px, 0.78fr);
+    grid-template-columns: minmax(300px, 0.95fr) minmax(330px, 1.05fr);
     grid-template-rows: minmax(0, 1fr);
     column-gap: 14px;
     align-items: stretch;
@@ -518,6 +519,8 @@ body[data-theme="night"] .fleet-icon {
 .region-chart-wrap {
     min-height: 0;
     width: 100%;
+    max-width: 430px;
+    justify-self: center;
     display: flex;
     align-items: center;
     justify-content: center;
@@ -545,7 +548,7 @@ body[data-theme="night"] .fleet-icon {
     gap: 7px;
     align-items: stretch;
     align-self: stretch;
-    padding: 10px;
+    padding: 9px;
     overflow: hidden;
     border: 1px solid rgba(117, 148, 188, 0.2);
     border-radius: 15px;
@@ -592,10 +595,12 @@ body[data-theme="night"] .fleet-icon {
 
 .region-ranking-list {
     min-height: 0;
+    flex: 1 1 auto;
     display: flex;
     flex-direction: column;
     gap: 4px;
-    overflow: hidden;
+    overflow-y: auto;
+    overflow-x: hidden;
 }
 
 .region-ranking-row {
@@ -1030,7 +1035,7 @@ body[data-theme="day"] .stat-icon {
 }
 
 body[data-theme="day"] .operator-name,
-body[data-theme="day"] .kpi-card p,
+body[data-theme="day"] .kpi-card h3,
 body[data-theme="day"] .item-tipo,
 body[data-theme="day"] .item-reg {
     color: #142535;
@@ -1330,7 +1335,7 @@ body[data-theme="day"] .smart-modal-field {
     background: rgba(229, 231, 234, 0.72);
 }
 
-@media (min-width: 1518px) {
+@media (min-width: 1519px) {
     body {
         padding: 20px 24px 24px;
     }
@@ -1359,31 +1364,32 @@ body[data-theme="day"] .smart-modal-field {
     .kpi-container {
         width: 100%;
         max-width: none;
-        gap: 14px;
+        gap: 10px;
         grid-template-columns: repeat(5, minmax(0, 1fr));
     }
 
     .kpi-card {
-        min-height: 126px;
-        padding: 16px 18px;
-        border-radius: 16px;
+        min-height: 76px;
+        padding: 8px 14px;
+        border-radius: 12px;
     }
 
     .kpi-card h3 {
-        font-size: 0.95rem;
+        font-size: 0.68rem;
     }
 
-    .kpi-card .value {
-        font-size: clamp(2.2rem, 2.6vw, 3.7rem);
+    .kpi-card > div:last-child {
+        grid-template-rows: minmax(2.35rem, auto) minmax(1.55rem, auto);
     }
 
-    .kpi-card p {
-        font-size: 0.86rem;
+    .kpi-card .value {
+        min-height: 2.35rem;
+        font-size: clamp(1.62rem, 1.95vw, 2.55rem);
     }
 
     .main-content {
         gap: 16px;
-        grid-template-columns: minmax(620px, 1.7fr) minmax(360px, 0.95fr) minmax(320px, 0.88fr);
+        grid-template-columns: minmax(620px, 1.9fr) minmax(300px, 0.75fr) minmax(330px, 0.78fr);
     }
 
     .panel-card {
@@ -1396,7 +1402,7 @@ body[data-theme="day"] .smart-modal-field {
     }
 
     .region-panel {
-        grid-template-columns: minmax(470px, 1.15fr) minmax(330px, 0.8fr);
+        grid-template-columns: minmax(430px, 1fr) minmax(390px, 1fr);
         column-gap: 18px;
         align-items: center;
     }
@@ -1415,22 +1421,27 @@ body[data-theme="day"] .smart-modal-field {
     }
 
     .region-visual,
-    .type-panel,
+    .type-panel {
+        min-height: 0;
+        height: 100%;
+    }
+
     .active-list-section {
         min-height: 0;
         height: 100%;
     }
 
     .region-chart-wrap {
-        min-height: 470px;
+        min-height: 420px;
+        max-width: 500px;
     }
 
     .region-ranking-panel {
-        min-height: 470px;
+        min-height: 420px;
         height: auto;
-        max-height: 520px;
-        padding: 12px;
-        gap: 8px;
+        max-height: 500px;
+        padding: 10px;
+        gap: 6px;
     }
 
     .region-ranking-title {
@@ -1438,14 +1449,14 @@ body[data-theme="day"] .smart-modal-field {
     }
 
     .region-ranking-head {
-        font-size: 0.72rem;
-        padding: 0 10px 6px 54px;
+        font-size: 0.54rem;
+        padding: 0;
     }
 
     .region-ranking-row {
-        min-height: 58px;
-        padding: 7px 8px;
-        font-size: 0.6rem;
+        min-height: 54px;
+        padding: 0;
+        font-size: 0.56rem;
     }
 
     .region-rank {
@@ -1494,6 +1505,14 @@ body[data-theme="day"] .smart-modal-field {
         padding: 14px;
     }
 
+    .type-chart-wrap canvas {
+        max-width: 100%;
+    }
+
+    .type-chart-wrap .chartjs-render-monitor {
+        font-size: 1rem;
+    }
+
     .list-container {
         flex: 1 1 auto;
         min-height: 0;
@@ -1514,54 +1533,6 @@ body[data-theme="day"] .smart-modal-field {
         font-size: 0.84rem;
     }
 
-    .type-chart-wrap canvas {
-        max-width: 100%;
-    }
-
-    .type-chart-wrap .chartjs-render-monitor {
-        font-size: 1        .region-ranking-head {
-            display: grid;
-            grid-template-columns: minmax(0, 1fr) 64px;
-            gap: 6px;
-            padding: 0 8px 5px 42px;
-            border-bottom: 1px solid rgba(176, 202, 232, 0.28);
-            color: #142535;
-            font-size: 0.40625rem;  ← Aqui! (valor original)
-            font-weight: 950;
-            text-transform: uppercase;
-        }        .region-ranking-head {
-            display: grid;
-            grid-template-columns: minmax(0, 1fr) 64px;
-            gap: 6px;
-            padding: 0 8px 5px 42px;
-            border-bottom: 1px solid rgba(176, 202, 232, 0.28);
-            color: #142535;
-            font-size: 0.40625rem;  ← Aqui! (valor original)
-            font-weight: 950;
-            text-transform: uppercase;
-        }        .region-ranking-head {
-            display: grid;
-            grid-template-columns: minmax(0, 1fr) 64px;
-            gap: 6px;
-            padding: 0 8px 5px 42px;
-            border-bottom: 1px solid rgba(176, 202, 232, 0.28);
-            color: #142535;
-            font-size: 0.40625rem;  ← Aqui! (valor original)
-            font-weight: 950;
-            text-transform: uppercase;
-        }        .region-ranking-head {
-            display: grid;
-            grid-template-columns: minmax(0, 1fr) 64px;
-            gap: 6px;
-            padding: 0 8px 5px 42px;
-            border-bottom: 1px solid rgba(176, 202, 232, 0.28);
-            color: #142535;
-            font-size: 0.40625rem;  ← Aqui! (valor original)
-            font-weight: 950;
-            text-transform: uppercase;
-        }rem;
-    }
-
     .region-fleet-counts img,
     .fleet-icon {
         width: 18px;
@@ -1622,7 +1593,7 @@ body[data-theme="day"] .smart-modal-field {
 
     .kpi-card {
         flex: 0 0 min(78vw, 310px);
-        min-height: 150px;
+        min-height: 92px;
         scroll-snap-align: start;
     }
 
@@ -1834,24 +1805,24 @@ body[data-theme="day"] .smart-modal-field {
     .kpi-card {
         flex-basis: min(78vw, 260px);
         max-width: min(78vw, 260px);
-        grid-template-columns: 52px minmax(0, 1fr);
-        min-height: 132px;
-        padding: 14px;
+        grid-template-columns: 40px minmax(0, 1fr);
+        min-height: 86px;
+        padding: 8px 10px;
     }
 
     .stat-icon {
-        width: 52px;
-        height: 52px;
+        width: 40px;
+        height: 40px;
     }
 
     .stat-icon svg {
-        width: 29px;
-        height: 29px;
+        width: 23px;
+        height: 23px;
     }
 
     .stat-icon img {
-        width: 31px;
-        height: 31px;
+        width: 24px;
+        height: 24px;
     }
 
     .main-content {
@@ -1906,11 +1877,6 @@ body[data-theme="day"] .smart-modal-field {
         padding-left: 38px;
     }
 
-    .type-chart-wrap {
-        min-height: 280px;
-        padding: 8px;
-    }
-
     .list-item {
         grid-template-columns: 12px auto minmax(0, 1fr);
         gap: 8px;
@@ -1947,8 +1913,7 @@ body[data-theme="day"] .smart-modal-field {
 
 }
 
-/* Aumentar font-size do region-ranking-panel para telas 1517px até tablet */
-@media (max-width: 1517px) and (min-width: 769px) {
+@media (max-width: 1518px) and (min-width: 769px) {
     .region-ranking-head {
         font-size: 0.65rem;
     }
@@ -1973,3 +1938,466 @@ body[data-theme="day"] .smart-modal-field {
         font-size: 0.95rem;
     }
 }
+
+.shift-filter {
+    display: grid;
+    grid-template-columns: repeat(5, minmax(0, 1fr));
+    gap: 6px;
+    margin-top: 8px;
+}
+
+.shift-filter-btn {
+    min-width: 0;
+    min-height: 34px;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    padding: 6px 8px;
+    border: 1px solid color-mix(in srgb, var(--shift-color) 62%, transparent);
+    border-radius: 8px;
+    color: var(--sw-text);
+    background: color-mix(in srgb, var(--shift-color) 10%, rgba(8, 22, 38, 0.68));
+    box-shadow: inset 0 -2px 0 color-mix(in srgb, var(--shift-color) 46%, transparent);
+    cursor: pointer;
+    font: inherit;
+}
+
+.shift-filter-btn.active {
+    background: color-mix(in srgb, var(--shift-color) 22%, rgba(8, 22, 38, 0.78));
+    box-shadow: 0 0 12px color-mix(in srgb, var(--shift-color) 28%, transparent), inset 0 -3px 0 color-mix(in srgb, var(--shift-color) 70%, transparent);
+}
+
+.shift-filter-btn strong {
+    overflow: hidden;
+    font-size: 0.64rem;
+    font-weight: 950;
+    line-height: 1;
+    text-overflow: ellipsis;
+    text-transform: uppercase;
+    white-space: nowrap;
+}
+
+.region-ranking-row {
+    grid-template-columns: minmax(0, 1fr) !important;
+    align-items: stretch;
+    overflow: visible;
+    min-height: 54px;
+    height: auto;
+    padding: 0;
+    border-left-width: 3px;
+    border-radius: 7px;
+    background: linear-gradient(180deg, rgba(6, 22, 40, 0.96), rgba(2, 13, 27, 0.96));
+    color: #eaf4ff;
+}
+
+.region-ranking-row-empty {
+    min-height: 32px;
+}
+
+.region-ranking-row-empty .region-info > * {
+    padding-top: 2px;
+    padding-bottom: 2px;
+}
+
+.region-ranking-row-empty .region-agents {
+    display: none;
+}
+
+.region-ranking-row-empty .region-fleet-panel {
+    justify-content: center;
+}
+
+.region-ranking-row-empty .region-name-line .region-name {
+    font-size: 0.82rem;
+}
+
+.region-ranking-row-empty .region-name-line .region-name-general {
+    display: inline-block;
+    zoom: 0.72;
+    line-height: 1;
+    font-weight: 950;
+}
+
+.region-ranking-row-empty .region-occurrence-cell strong {
+    font-size: 1rem;
+}
+
+.region-ranking-head {
+    grid-template-columns: 58px 76px minmax(96px, 1fr) minmax(96px, 1fr);
+    align-items: center;
+    gap: 0;
+    padding: 0;
+    min-height: 22px;
+    overflow: hidden;
+    border: 1px solid rgba(91, 176, 255, 0.18);
+    border-radius: 7px 7px 0 0;
+    background: rgba(4, 18, 34, 0.94);
+    color: #dce8f8;
+    font-size: 0.46rem;
+    letter-spacing: 0;
+}
+
+.region-ranking-head span {
+    min-width: 0;
+    height: 100%;
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    padding: 0 4px;
+    border-left: 1px solid rgba(91, 176, 255, 0.14);
+    overflow: hidden;
+    text-align: center;
+    text-overflow: ellipsis;
+    white-space: nowrap;
+}
+
+.region-ranking-head span:first-child {
+    border-left: 0;
+}
+
+.region-info {
+    min-width: 0;
+    display: grid;
+    grid-template-columns: 58px 76px minmax(96px, 1fr) minmax(96px, 1fr);
+    align-items: stretch;
+    gap: 0;
+    height: 100%;
+}
+
+.region-info > * {
+    min-width: 0;
+    padding: 4px 6px;
+    border-left: 1px solid rgba(91, 176, 255, 0.14);
+}
+
+.region-info > *:first-child {
+    border-left: 0;
+}
+
+.region-info > .region-name-line ~ .region-name-line {
+    display: none;
+}
+
+.region-name-line {
+    justify-self: center;
+    display: inline-flex;
+    width: 100%;
+    min-width: 0;
+    max-width: none;
+    flex-direction: column;
+    align-items: center;
+    justify-content: center;
+    gap: 1px;
+    padding: 4px 5px;
+    border: 1px solid var(--legend-color);
+    border-radius: 7px;
+    color: #ffffff;
+    background: color-mix(in srgb, var(--legend-color) 78%, rgba(5, 18, 34, 0.9));
+    text-align: center;
+}
+
+.region-name-line > span {
+    font-size: 0.38rem;
+    font-weight: 950;
+    line-height: 1;
+}
+
+.region-name-line .region-name {
+    color: #ffffff !important;
+    font-size: 0.74rem;
+    line-height: 1;
+    white-space: normal;
+}
+
+.region-name-line .region-name-general {
+    display: inline-block;
+    zoom: 0.72;
+    line-height: 1;
+    font-weight: 950;
+}
+
+.region-occurrence-cell {
+    display: flex;
+    flex-direction: column;
+    align-items: center;
+    justify-content: center;
+    color: #ffffff;
+    text-align: center;
+}
+
+.region-occurrence-cell strong {
+    font-size: 1.2rem;
+    line-height: 0.95;
+}
+
+.region-occurrence-cell span,
+.region-empty-agent {
+    color: #8ea5bc;
+    font-size: 0.5rem;
+    font-weight: 900;
+    line-height: 1;
+}
+
+.region-fleet-panel {
+    min-width: 0;
+    display: flex;
+    flex-direction: column;
+    justify-content: flex-start;
+    gap: 2px;
+    overflow: visible;
+}
+
+.region-fleet-vt {
+    align-items: flex-start;
+}
+
+.region-fleet-mt {
+    align-items: flex-start;
+}
+
+.region-fleet-count {
+    display: inline-flex;
+    align-items: center;
+    gap: 4px;
+    color: inherit;
+    font-size: 0.5rem;
+    font-weight: 950;
+    line-height: 1;
+    white-space: nowrap;
+}
+
+.region-fleet-count strong {
+    font-size: 0.5rem;
+}
+
+.region-ranking-panel .region-fleet-count img,
+.region-ranking-panel .region-total-fleet img {
+    width: 14px;
+    height: 14px;
+    flex: 0 0 14px;
+    object-fit: contain;
+}
+
+.region-agents {
+    min-width: 0;
+    display: flex;
+    flex-direction: column;
+    gap: 2px;
+    max-height: none;
+    overflow: visible;
+}
+
+.region-agent-group {
+    min-width: 0;
+    display: inline-flex;
+    align-items: flex-start;
+    gap: 2px;
+    max-width: 100%;
+    overflow: visible;
+}
+
+.region-agent-group img {
+    width: 8px;
+    height: 8px;
+    flex: 0 0 8px;
+    object-fit: contain;
+}
+
+.region-agent-group > span {
+    min-width: 0;
+    display: flex;
+    flex-direction: column;
+    max-height: none;
+    overflow: visible;
+}
+
+.region-agent-group-active {
+    padding: 1px;
+    border: 1px solid color-mix(in srgb, var(--agent-color) 64%, transparent);
+    border-radius: 4px;
+    background: color-mix(in srgb, var(--agent-color) 22%, transparent);
+    max-height: none;
+}
+
+.region-agent-name,
+.region-agent-code {
+    display: block;
+    max-width: 100%;
+    overflow: hidden;
+    text-overflow: ellipsis;
+    white-space: nowrap;
+}
+
+.region-agent-code {
+    color: #dce8f8;
+    font-size: 0.49rem;
+    font-weight: 950;
+    line-height: 1;
+}
+
+.region-agent-name {
+    color: #dce8f8;
+    font-size: 0.49rem;
+    font-weight: 900;
+    line-height: 1;
+}
+
+button.region-agent-name {
+    padding: 0;
+    border: 0;
+    color: inherit;
+    background: transparent;
+    font: inherit;
+    text-align: left;
+    cursor: pointer;
+}
+
+.region-agent-name-available {
+    color: #20c563 !important;
+}
+
+.region-agent-name-unavailable {
+    color: #8f9aa3 !important;
+    font-size: 0.49rem !important;
+    font-weight: 900;
+    line-height: 1;
+    text-decoration: underline;
+    text-decoration-thickness: 1px;
+    text-underline-offset: 2px;
+}
+
+button.region-agent-name.region-agent-name-unavailable {
+    font-size: 0.49rem !important;
+    line-height: 1;
+}
+
+.region-agent-group-active .region-agent-name,
+.region-agent-group-active .region-agent-code {
+    color: #ff2f45 !important;
+    animation: regionAgentAttendBlink 0.9s ease-in-out infinite;
+}
+
+@keyframes regionAgentAttendBlink {
+    0%,
+    100% {
+        opacity: 1;
+        text-shadow: 0 0 7px rgba(255, 47, 69, 0.72);
+    }
+
+    50% {
+        opacity: 0.38;
+        text-shadow: none;
+    }
+}
+
+.region-ranking-total {
+    grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
+}
+
+.region-total-fleet {
+    display: inline-flex;
+    align-items: center;
+    gap: 5px;
+}
+
+.region-total-mt {
+    justify-self: end;
+}
+
+.region-total-label {
+    justify-self: center;
+}
+
+.list-item.zona-atendimento-1 {
+    --zona-bg: rgba(59, 54, 76, 0.92);
+    --zona-bg-strong: #3b364c;
+    --zona-border: rgba(99, 93, 122, 0.82);
+    --zona-text: #ffffff;
+    --zona-time-bg: rgba(255, 255, 255, 0.18);
+}
+
+.list-item.zona-atendimento-2 {
+    --zona-bg: rgba(255, 204, 0, 0.82);
+    --zona-bg-strong: #ffcc00;
+    --zona-border: rgba(255, 246, 134, 0.92);
+    --zona-text: #101820;
+    --zona-time-bg: rgba(16, 24, 32, 0.12);
+}
+
+.list-item.zona-atendimento-3 {
+    --zona-bg: rgba(85, 200, 10, 0.84);
+    --zona-bg-strong: #55c80a;
+    --zona-border: rgba(168, 255, 102, 0.82);
+    --zona-text: #071525;
+    --zona-time-bg: rgba(7, 21, 37, 0.12);
+}
+
+.list-item.zona-atendimento-4 {
+    --zona-bg: rgba(17, 136, 255, 0.84);
+    --zona-bg-strong: #1188ff;
+    --zona-border: rgba(119, 190, 255, 0.82);
+    --zona-text: #ffffff;
+    --zona-time-bg: rgba(255, 255, 255, 0.18);
+}
+
+.list-item.zona-atendimento-5 {
+    --zona-bg: rgba(229, 6, 6, 0.84);
+    --zona-bg-strong: #e50606;
+    --zona-border: rgba(255, 112, 112, 0.82);
+    --zona-text: #ffffff;
+    --zona-time-bg: rgba(255, 255, 255, 0.18);
+}
+
+.list-item.zona-atendimento-geral {
+    --zona-bg: rgba(141, 85, 217, 0.82);
+    --zona-bg-strong: #8d55d9;
+    --zona-border: rgba(186, 146, 255, 0.72);
+    --zona-text: #ffffff;
+    --zona-time-bg: rgba(255, 255, 255, 0.18);
+}
+
+.list-item.zona-atendimento-sem-regiao {
+    --zona-bg: rgba(88, 108, 128, 0.68);
+    --zona-bg-strong: rgba(44, 60, 78, 0.88);
+    --zona-border: rgba(156, 176, 198, 0.58);
+    --zona-text: #ffffff;
+    --zona-time-bg: rgba(255, 255, 255, 0.14);
+}
+
+.list-item[class*="zona-atendimento-"] {
+    border-color: var(--zona-border) !important;
+    background: linear-gradient(135deg, var(--zona-bg), var(--zona-bg-strong)) !important;
+    filter: none !important;
+}
+
+.list-item[class*="zona-atendimento-"] .item-tipo,
+body[data-theme="day"] .list-item[class*="zona-atendimento-"] .item-tipo,
+.list-item[class*="zona-atendimento-"] .item-hora {
+    color: var(--zona-text) !important;
+}
+
+.list-item[class*="zona-atendimento-"] .item-hora {
+    background: var(--zona-time-bg);
+    border-color: color-mix(in srgb, var(--zona-border) 76%, transparent);
+}
+
+@media (max-width: 768px) {
+    .shift-filter {
+        grid-template-columns: repeat(2, minmax(0, 1fr));
+    }
+
+    .region-info {
+        grid-template-columns: 1fr;
+    }
+
+    .region-name-line {
+        justify-self: stretch;
+        max-width: none;
+    }
+
+    .region-agent-name,
+    .region-agent-code {
+        max-width: none;
+    }
+}
+
diff --git a/js/agentes-2.js b/js/agentes-2.js
index 1473504..f3968ba 100644
--- a/js/agentes-2.js
+++ b/js/agentes-2.js
@@ -99,10 +99,7 @@
                         // 2. INJETAR CSS PARA ESCONDER AÇÕES NA TABELA
                         const styleBlock = document.createElement('style');
                         styleBlock.innerHTML = `
-                            .col-acao, .btn-acao-tabela, .btn-encaminhar, .btn-devolver, .btn-confirmar {
-                                display: none !important;
-                            }
-                            table th:last-child, table td:last-child {
+                            .col-acao, .btn-acao-tabela, .btn-encaminhar, .btn-devolver, .btn-confirmar, .btn-editar-ativo {
                                 display: none !important;
                             }
                         `;
@@ -422,6 +419,113 @@
             .replace(/"/g, "&quot;");
     }
 
+    function isVeiculoMtOuVt(valor) {
+        return /^(MT|VT)\b/i.test(String(valor || "").trim());
+    }
+
+    function isDisponivelSmartwall(dados) {
+        return dados?.disponivelSmartwall !== false;
+    }
+
+    function solicitarMotivoIndisponibilidade(dados) {
+        return new Promise((resolve) => {
+            let modal = document.getElementById('modalMotivoIndisponibilidade');
+            if (!modal) {
+                modal = document.createElement('div');
+                modal.id = 'modalMotivoIndisponibilidade';
+                modal.className = 'modal-motivo-indisponibilidade';
+                modal.innerHTML = `
+                    <div class="modal-motivo-backdrop" data-motivo-cancelar></div>
+                    <section class="modal-motivo-card" role="dialog" aria-modal="true" aria-labelledby="modalMotivoTitulo">
+                        <h2 id="modalMotivoTitulo">Motivo da indisponibilidade</h2>
+                        <label>
+                            <span>Informe o motivo</span>
+                            <textarea id="motivoIndisponibilidadeTexto" maxlength="240" placeholder="Digite o motivo..." spellcheck="true" lang="pt-BR"></textarea>
+                        </label>
+                        <div class="modal-motivo-actions">
+                            <button type="button" class="btn-motivo-cancelar" data-motivo-cancelar>Cancelar</button>
+                            <button type="button" class="btn-motivo-confirmar" data-motivo-confirmar>Salvar</button>
+                        </div>
+                    </section>
+                `;
+                document.body.appendChild(modal);
+            }
+
+            const textarea = modal.querySelector('#motivoIndisponibilidadeTexto');
+            const titulo = modal.querySelector('#modalMotivoTitulo');
+            const finalizar = (valor) => {
+                modal.classList.remove('open');
+                modal.querySelectorAll('[data-motivo-cancelar], [data-motivo-confirmar]').forEach((botao) => {
+                    botao.onclick = null;
+                });
+                resolve(valor);
+            };
+
+            titulo.innerText = `Motivo da indisponibilidade - ${dados.veiculo || 'Equipe'}`;
+            textarea.value = dados.motivoIndisponibilidadeSmartwall || "";
+            modal.classList.add('open');
+            setTimeout(() => textarea.focus(), 0);
+
+            modal.querySelectorAll('[data-motivo-cancelar]').forEach((botao) => {
+                botao.onclick = () => finalizar(null);
+            });
+            modal.querySelector('[data-motivo-confirmar]').onclick = () => {
+                const motivo = textarea.value.trim();
+                if (!motivo) {
+                    alert("Informe o motivo da indisponibilidade.");
+                    textarea.focus();
+                    return;
+                }
+                finalizar(motivo.toUpperCase());
+            };
+        });
+    }
+
+    function criarBotaoDisponibilidadeSmartwall(dados) {
+        const btnDisponibilidade = document.createElement('button');
+        btnDisponibilidade.type = 'button';
+        btnDisponibilidade.className = 'btn btn-disponibilidade-smartwall';
+
+        const atualizarVisual = (disponivel) => {
+            btnDisponibilidade.dataset.disponivel = String(disponivel);
+            btnDisponibilidade.innerText = disponivel ? 'DISPONÍVEL' : 'INDISPONÍVEL';
+            btnDisponibilidade.title = disponivel
+                ? 'Marcar agentes como indisponíveis no Smartwall'
+                : 'Marcar agentes como disponíveis no Smartwall';
+            btnDisponibilidade.setAttribute('aria-label', btnDisponibilidade.title);
+        };
+
+        atualizarVisual(isDisponivelSmartwall(dados));
+
+        btnDisponibilidade.onclick = async function() {
+            const novoStatus = btnDisponibilidade.dataset.disponivel !== 'true';
+            const motivo = novoStatus ? "" : await solicitarMotivoIndisponibilidade(dados);
+            if (motivo === null) return;
+
+            btnDisponibilidade.disabled = true;
+            atualizarVisual(novoStatus);
+
+            try {
+                await updateDoc(doc(db, "ativos_agentes", dados.id), {
+                    disponivelSmartwall: novoStatus,
+                    motivoIndisponibilidadeSmartwall: motivo,
+                    atualizadoDisponibilidadeSmartwallEm: serverTimestamp()
+                });
+                registrarLogAuditoria(
+                    "DISPONIBILIDADE SMARTWALL",
+                    `Veículo: ${dados.veiculo} marcado como ${novoStatus ? "DISPONÍVEL" : "INDISPONÍVEL"} por ${nomeUsuarioLogado}.${motivo ? " Motivo: " + motivo : ""}`
+                );
+            } catch (e) {
+                atualizarVisual(!novoStatus);
+                alert("Erro ao atualizar disponibilidade: " + e.message);
+            } finally {
+                btnDisponibilidade.disabled = false;
+            }
+        };
+
+        return btnDisponibilidade;
+    }
+
     function normalizarBuscaAgente(valor) {
         return String(valor || "")
             .normalize("NFD")
@@ -877,6 +981,10 @@
             const actionCell = row.insertCell();
             actionCell.className = 'acao-botoes';
             actionCell.appendChild(criarBotaoVisualizarRegistro(dados, "Veículo em Operação"));
+
+            if (isVeiculoMtOuVt(dados.veiculo)) {
+                actionCell.appendChild(criarBotaoDisponibilidadeSmartwall(dados));
+            }
             
             // --- AQUI VEM O BLOQUEIO: Só cria botões se NÃO for visualizador
             if (!isVisualizador) {
@@ -988,6 +1096,7 @@
                             // ATUALIZAÇÃO NO BANCO (INCLUINDO HT E ASE)
                             await updateDoc(doc(db, "ativos_agentes", dados.id), {
                                 zona: novaZona,
+                                zonaClasse: obterClasseRegiao(novaZona),
                                 horaInicio: horaCorte,
                                 agente: novaLista.join('\n'),
                                 situacao: cellsMap['situacao'].innerText.trim().toUpperCase(),
@@ -1147,6 +1256,7 @@
                             situacao: cellsMap.situacao.innerText.trim().toUpperCase(),
                             maletas: cellsMap.maletas.innerText.trim().toUpperCase(),
                             zona: novaZona,
+                            zonaClasse: obterClasseRegiao(novaZona),
                             horaInicio: formatarHoraComSegundos(cellsMap.horaInicio.innerText.trim()),
                             pontoBase: cellsMap.pontoBase.innerText.trim().toUpperCase()
                         };
@@ -1217,6 +1327,7 @@
                 situacao: document.getElementById('situacao').value,
                 ht: valoresHT,
                 zona: z,
+                zonaClasse: obterClasseRegiao(z),
                 horaInicio: document.getElementById('horaInicio').value,
                 ase: valoresASE,
                 maletas: maletasVal.toUpperCase(),
diff --git a/js/smartwall.js b/js/smartwall.js
index 8ad8110..e051c2c 100644
--- a/js/smartwall.js
+++ b/js/smartwall.js
@@ -22,11 +22,20 @@ async function iniciarSmartwall() {
     const STATUS_DESPACHO = new Set(["PARA O DESPACHO"]);
     const STATUS_EM_ANDAMENTO = new Set(["ENCAMINHADA", "EM ANDAMENTO"]);
     const STATUS_ATIVOS_SMARTWALL = new Set([...STATUS_DESPACHO, ...STATUS_EM_ANDAMENTO]);
-    const regioesBase = ["GERAL", "REGI\u00c3O 1", "REGI\u00c3O 2", "REGI\u00c3O 3", "REGI\u00c3O 4", "REGI\u00c3O 5"];
+    const regioesPainel = ["REGI\u00c3O 1", "REGI\u00c3O 2", "REGI\u00c3O 3", "REGI\u00c3O 4", "REGI\u00c3O 5", "GERAL"];
+    const regioesBase = regioesPainel;
     const coresRegiao = ["#67479a", "#3b364c", "#e4f001", "#64ec02", "#096acc", "#d30000"];
+    const coresPainelRegiao = ["#3b364c", "#ffcc00", "#55c80a", "#1188ff", "#e50606", "#8d55d9"];
     const coresRegiaoVt = ["#8060b2", "#554e6c", "#f2ff3b", "#80ff28", "#2387e8", "#ff2a2a"];
     const coresRegiaoMt = ["#4b3274", "#292639", "#aab300", "#43aa00", "#064a91", "#8f0000"];
     const coresTipo = ["#2f86ff", "#ffbd1a", "#64d637", "#ff4c5d", "#9d55ff"];
+    const turnosAtendimento = [
+        { key: "total", label: "TOTAL", range: "Historico + abertos", icon: "src/total_png.png", color: "#2be477" },
+        { key: "manha", label: "MANHÃ", range: "06:00:00 às 11:59:59", inicio: 21600, fim: 43199, icon: "src/andamento_png.png", color: "#1188ff" },
+        { key: "tarde", label: "TARDE", range: "12:00:00 às 17:59:59", inicio: 43200, fim: 64799, icon: "src/total_png.png", color: "#ff9f1c" },
+        { key: "noite", label: "NOITE", range: "18:00:00 às 23:59:59", inicio: 64800, fim: 86399, icon: "src/concluido_png.png", color: "#9b5dff" },
+        { key: "corujao", label: "CORUJÃO", range: "00:00:00 às 05:59:59", inicio: 0, fim: 21599, icon: "src/live_png.png", color: "#16c8d8" }
+    ];
     const mtIconImg = new Image();
     const vtIconImg = new Image();
     mtIconImg.src = "src/mt_png.png";
@@ -34,6 +43,11 @@ async function iniciarSmartwall() {
 
     let myChartRegioes = null;
     let myChartTipos = null;
+    let turnoAtendimentoAtual = getTurnoAtualKey();
+    let ultimosDocsAtivosAgentes = [];
+    let ultimosDocsHistoricoAgentes = [];
+    let ultimasOcorrenciasAtivas = [];
+    let ultimaContagemRegiaoAtivas = {};
     const ocorrenciasModal = new Map();
     const kpiValoresAnteriores = {
         total: null,
@@ -114,6 +128,54 @@ async function iniciarSmartwall() {
         return Number(match[1]) * 60 + Number(match[2]);
     }
 
+    function getHoraSegundos(hora) {
+        const match = String(hora || "").trim().match(/^(\d{1,2}):(\d{2})(?::(\d{2}))?/);
+        if (!match) return -1;
+        return (Number(match[1]) * 3600) + (Number(match[2]) * 60) + Number(match[3] || 0);
+    }
+
+    function getTurnoAtualKey() {
+        const agora = new Date();
+        const segundos = (agora.getHours() * 3600) + (agora.getMinutes() * 60) + agora.getSeconds();
+        const turno = turnosAtendimento.find((item) => item.key !== "total" && segundos >= item.inicio && segundos <= item.fim);
+        return turno?.key || "manha";
+    }
+
+    function itemNoTurnoAtendimento(item, turnoKey) {
+        if (turnoKey === "total") return true;
+        const turno = turnosAtendimento.find((opcao) => opcao.key === turnoKey);
+        if (!turno) return true;
+        const segundosInicio = getHoraSegundos(item?.horaInicio || item?.inicio || item?.horaEnvio);
+        if (segundosInicio < 0) return true;
+        return segundosInicio >= turno.inicio && segundosInicio <= turno.fim;
+    }
+
+    function filtrarPorTurnoAtendimento(registros) {
+        return registros.filter((item) => itemNoTurnoAtendimento(item, turnoAtendimentoAtual));
+    }
+
+    function somarFrotasPorRegiao(...frotas) {
+        const base = Object.fromEntries(regioesBase.map((regiao) => [regiao, { mt: 0, vt: 0 }]));
+        frotas.forEach((frota) => {
+            regioesBase.forEach((regiao) => {
+                base[regiao].mt += frota?.[regiao]?.mt || 0;
+                base[regiao].vt += frota?.[regiao]?.vt || 0;
+            });
+        });
+        return base;
+    }
+
+    function calcularContagemRegiao(registros) {
+        const contagem = Object.fromEntries(regioesBase.map((regiao) => [regiao, 0]));
+        registros.forEach((registro) => {
+            const zona = getZona(registro);
+            if (contagem[zona] !== undefined) {
+                contagem[zona]++;
+            }
+        });
+        return contagem;
+    }
+
     function formatarHoraComSegundos(hora) {
         const match = String(hora || "").trim().match(/^(\d{1,2}):(\d{2})(?::(\d{2}))?/);
         if (!match) return "--:--:--";
@@ -188,10 +250,39 @@ async function iniciarSmartwall() {
     }
 
     function getZona(ocorrencia) {
-        return normalizarTexto(ocorrencia?.zona || ocorrencia?.regiao || "SEM REGI\u00c3O");
+        const referenciasZona = [
+            ocorrencia?.classeRegiao,
+            ocorrencia?.zonaClasse,
+            ocorrencia?.className,
+            ocorrencia?.zona,
+            ocorrencia?.regiao
+        ];
+        if (referenciasZona.some((valor) => normalizarZona(valor) === "GERAL")) return "GERAL";
+        return normalizarZona(referenciasZona.find((valor) => String(valor || "").trim()) || "SEM REGI\u00c3O");
+    }
+
+    function normalizarZona(valor) {
+        const zona = normalizarTexto(valor);
+        const zonaSemAcento = zona.normalize("NFD").replace(/[\u0300-\u036f]/g, "");
+        if (zonaSemAcento.includes("GERAL") || zonaSemAcento.includes("REGIAO-GERAL")) return "GERAL";
+        const numero = zonaSemAcento.match(/\b(?:REGIAO|ZONA|REGIAO-)\s*([1-6])\b/)?.[1];
+        if (numero === "6") return "GERAL";
+        if (numero) return `REGI\u00c3O ${numero}`;
+        return zona;
+    }
+
+    function getClasseZonaLista(ocorrencia) {
+        const zona = getZona(ocorrencia);
+        const numero = String(zona || "").match(/[1-5]/)?.[0];
+        if (numero) return `zona-atendimento-${numero}`;
+        if (normalizarZona(zona) === "GERAL") return "zona-atendimento-geral";
+        return "zona-atendimento-sem-regiao";
     }
 
     function atualizarDashboard(docsHoje, docsPendentes, docsAtivosAgentes, docsHistoricoAgentes) {
+        ultimosDocsAtivosAgentes = docsAtivosAgentes;
+        ultimosDocsHistoricoAgentes = docsHistoricoAgentes;
+
         const mapaUnico = new Map();
         docsPendentes.forEach((item) => mapaUnico.set(item.id, item));
         docsHoje.forEach((item) => mapaUnico.set(item.id, item));
@@ -199,101 +290,93 @@ async function iniciarSmartwall() {
         const todas = Array.from(mapaUnico.values());
         const hoje = todas.filter((ocorrencia) => ocorrencia.data_filtro === dataFiltro);
         const ativas = todas.filter(isAtivaNoPainelPendentes).sort(ordenarRecentes);
-        const ativasHoje = ativas.filter((ocorrencia) => ocorrencia.data_filtro === dataFiltro);
+        ultimasOcorrenciasAtivas = ativas;
         const concluidasHoje = hoje.filter(isConcluida);
         const qtdDespacho = ativas.filter((ocorrencia) => STATUS_DESPACHO.has(normalizarTexto(ocorrencia.situacao))).length;
         const qtdAndamento = ativas.filter((ocorrencia) => STATUS_EM_ANDAMENTO.has(normalizarTexto(ocorrencia.situacao))).length;
 
-        const contagemRegiaoAtivas = Object.fromEntries(regioesBase.map((regiao) => [regiao, 0]));
-        const contagemRegiaoHistorico = Object.fromEntries(regioesBase.map((regiao) => [regiao, 0]));
         const contagemTipo = {};
         const frotaAtiva = calcularFrotaAtiva(docsAtivosAgentes);
-        const frotaAtivaPorRegiao = calcularFrotaPorRegiao(docsAtivosAgentes);
-        const frotaHistoricoPorRegiao = calcularFrotaPorRegiao(docsHistoricoAgentes);
+        const docsHistoricoTurno = filtrarPorTurnoAtendimento(docsHistoricoAgentes);
+        const docsAtivosAgentesTurno = filtrarPorTurnoAtendimento(docsAtivosAgentes);
+        const ativasTurno = filtrarPorTurnoAtendimento(ativas);
+        const ativasHojeTurno = ativasTurno.filter((ocorrencia) => ocorrencia.data_filtro === dataFiltro);
+        const contagemRegiaoHistorico = calcularContagemRegiao(docsHistoricoTurno);
+        const contagemRegiaoAtivas = calcularContagemRegiao(ativasTurno);
+        const frotaAtivaPorRegiao = calcularFrotaPorRegiao(docsAtivosAgentesTurno);
+        const frotaHistoricoPorRegiao = calcularFrotaPorRegiao(docsHistoricoTurno);
+        const agentesPorRegiao = calcularAgentesPorRegiao(docsAtivosAgentesTurno, ativasTurno);
 
         hoje.forEach((ocorrencia) => {
             const tipoBase = getTipoBase(ocorrencia);
             contagemTipo[tipoBase] = (contagemTipo[tipoBase] || 0) + 1;
         });
 
-        docsHistoricoAgentes.forEach((registro) => {
-            const zona = getZona(registro);
-            if (contagemRegiaoHistorico[zona] !== undefined) {
-                contagemRegiaoHistorico[zona]++;
-            }
-        });
+        atualizarResumoAtendimento(ativasHojeTurno.length);
+        atualizarIndicadoresKpi(hoje.length, qtdDespacho, qtdAndamento, concluidasHoje.length, frotaAtiva);
+        atualizarListaAtivas(ativasHojeTurno.filter((ocorrencia) => ocorrencia.data_filtro === dataFiltro));
+        atualizarGraficos(
+            contagemRegiaoAtivas,
+            contagemRegiaoHistorico,
+            contagemTipo,
+            frotaAtivaPorRegiao,
+            frotaHistoricoPorRegiao,
+            agentesPorRegiao
+        );
+    }
 
-        ativas.forEach((ocorrencia) => {
-            const zona = getZona(ocorrencia);
-            if (contagemRegiaoAtivas[zona] !== undefined) {
-                contagemRegiaoAtivas[zona]++;
-            }
+    function atualizarIndicadoresKpi(total, despacho, andamento, concluidas, frota) {
+        const totalFrota = (frota?.mt || 0) + (frota?.vt || 0);
+        const pares = [
+            ["kpiTotal", total],
+            ["kpiDespacho", despacho],
+            ["kpiAndamento", andamento],
+            ["kpiConcluidas", concluidas],
+            ["kpiVtrs", totalFrota]
+        ];
+
+        pares.forEach(([id, valor]) => {
+            const el = document.getElementById(id);
+            if (el) el.textContent = String(valor ?? 0);
         });
 
-        atualizarKpiNumero("kpiTotal", concluidasHoje.length + ativas.length, "total", null, null);
-        atualizarKpiNumero("kpiDespacho", qtdDespacho, "despacho", "despacho", qtdDespacho, true);
-        atualizarKpiNumero("kpiAndamento", qtdAndamento, "andamento", "encaminhada", qtdAndamento, true);
-        atualizarKpiNumero("kpiConcluidas", concluidasHoje.length, "concluidas", "concluidaStatus", concluidasHoje.length, true);
-        document.getElementById("kpiVtrs").innerHTML = `
-            <span class="fleet-value fleet-mt"><img class="fleet-icon" src="src/mt_png.png" alt="MT"><span class="fleet-count">${frotaAtiva.mt}</span></span>
-            <span class="fleet-value fleet-vt"><img class="fleet-icon" src="src/vt_png.png" alt="VT"><span class="fleet-count">${frotaAtiva.vt}</span></span>
-        `;
-        atualizarKpiFrota(frotaAtiva);
-        atualizarResumoAtendimento(ativas.length);
-
-        atualizarListaAtivas(ativasHoje);
-        atualizarGraficos(contagemRegiaoAtivas, contagemRegiaoHistorico, contagemTipo, frotaAtivaPorRegiao, frotaHistoricoPorRegiao);
-    }
-
-    function atualizarKpiNumero(id, valor, chave, chaveAlerta = chave, valorAlerta = valor, alertarSomenteAumento = false) {
-        const elemento = document.getElementById(id);
-        if (!elemento) return;
-
-        const anterior = kpiValoresAnteriores[chave];
-        const anteriorAlerta = kpiValoresAnteriores[chaveAlerta];
-        elemento.innerText = valor;
-        const deveAlertar = chaveAlerta &&
-            anteriorAlerta !== null &&
-            anteriorAlerta !== valorAlerta &&
-            (!alertarSomenteAumento || valorAlerta > anteriorAlerta);
-        if (deveAlertar) {
-            const card = elemento.closest(".kpi-card");
-            if (card) {
-                card.classList.remove("kpi-alert");
-                void card.offsetWidth;
-                card.classList.add("kpi-alert");
-                setTimeout(() => card.classList.remove("kpi-alert"), 10000);
+        const totalCard = document.getElementById("kpiTotal")?.closest(".kpi-card");
+        if (totalCard) totalCard.classList.toggle("kpi-alert", total > 0);
+    }
+
+    function atualizarResumoAtendimento(qtdAtivas) {
+        const bloco = document.getElementById("activeLiveSummary");
+        if (!bloco) return;
+        const numero = bloco.querySelector("strong");
+        if (numero) numero.textContent = String(qtdAtivas);
+    }
+
+    function getTextoCampo(item, chaves) {
+        for (const chave of chaves) {
+            const valor = item?.[chave];
+            if (valor === null || valor === undefined || valor === "") continue;
+            if (Array.isArray(valor)) {
+                const textoArray = valor.map((parte) => String(parte || "").trim()).filter(Boolean).join(", ");
+                if (textoArray) return textoArray;
+                continue;
             }
+            return String(valor).trim();
         }
-        kpiValoresAnteriores[chave] = valor;
-        if (chaveAlerta) kpiValoresAnteriores[chaveAlerta] = valorAlerta;
-    }
-
-    function atualizarKpiFrota(frotaAtiva) {
-        const assinatura = `${frotaAtiva.mt}-${frotaAtiva.vt}`;
-        const anterior = kpiValoresAnteriores.frota;
-        const [mtAnterior = 0, vtAnterior = 0] = String(anterior || "0-0").split("-").map(Number);
-        if (anterior !== null) {
-            const card = document.getElementById("kpiVtrsCard");
-            if (frotaAtiva.mt !== mtAnterior) acionarPiscaFleet(card?.querySelector(".fleet-mt .fleet-icon"));
-            if (frotaAtiva.vt !== vtAnterior) acionarPiscaFleet(card?.querySelector(".fleet-vt .fleet-icon"));
-        }
-        kpiValoresAnteriores.frota = assinatura;
+        return "";
     }
 
-    function acionarPiscaFleet(icon) {
-        if (!icon) return;
-        icon.classList.remove("fleet-icon-alert");
-        void icon.offsetWidth;
-        icon.classList.add("fleet-icon-alert");
-        setTimeout(() => icon.classList.remove("fleet-icon-alert"), 10000);
+    function extrairAgentes(item) {
+        const candidato = getTextoCampo(item, ["nomes", "nome", "nomeCompleto", "nomeAgente", "agente", "responsavel", "motorista", "operador", "usuario", "equipe"]);
+        if (!candidato) return [];
+        return candidato.split(/[,;/|]+/).map((parte) => parte.trim()).filter(Boolean);
     }
 
-    function atualizarResumoAtendimento(total) {
-        const bloco = document.getElementById("activeLiveSummary");
-        if (!bloco) return;
-        const numero = bloco.querySelector("strong");
-        if (numero) numero.textContent = String(total);
+    function extrairCodigosEquipe(veiculo, tipo) {
+        const texto = Array.isArray(veiculo) ? veiculo.join(" ") : String(veiculo || "");
+        const normalizado = texto.toUpperCase();
+        if (normalizado.includes(tipo)) return [tipo];
+        const codigos = texto.match(/\b\d{1,4}\b/g) || [];
+        return codigos.length ? codigos : [tipo];
     }
 
     function calcularFrotaAtiva(docsAtivosAgentes) {
@@ -319,6 +402,47 @@ async function iniciarSmartwall() {
         return base;
     }
 
+    function calcularAgentesPorRegiao(docsAtivosAgentes, ocorrenciasAtivas = []) {
+        const ocorrenciasPorEquipe = new Map();
+        ocorrenciasAtivas.forEach((ocorrencia) => {
+            String(ocorrencia?.equipe || "")
+                .split(/[,;/|]+/)
+                .map((parte) => parte.trim())
+                .filter(Boolean)
+                .forEach((codigo) => {
+                    const lista = ocorrenciasPorEquipe.get(codigo) || [];
+                    lista.push(ocorrencia);
+                    ocorrenciasPorEquipe.set(codigo, lista);
+                });
+        });
+
+        const base = Object.fromEntries(regioesBase.map((regiao) => [regiao, { mt: [], vt: [] }]));
+        docsAtivosAgentes.forEach((item) => {
+            const zona = getZona(item);
+            if (!base[zona]) return;
+            const nomes = [...new Set(extrairAgentes(item))];
+            if (!nomes.length) return;
+            const disponivelSmartwall = item?.disponivelSmartwall !== false;
+            const motivoIndisponibilidadeSmartwall = item?.motivoIndisponibilidadeSmartwall || "";
+
+            extrairCodigosEquipe(item?.veiculo || item?.equipe || item?.codigoEquipe, "VT").forEach((codigo) => {
+                base[zona].vt.push({ codigo, nomes, disponivelSmartwall, motivoIndisponibilidadeSmartwall, ocorrencias: ocorrenciasPorEquipe.get(codigo) || [] });
+            });
+
+            extrairCodigosEquipe(item?.veiculo || item?.equipe || item?.codigoEquipe, "MT").forEach((codigo) => {
+                base[zona].mt.push({ codigo, nomes, disponivelSmartwall, motivoIndisponibilidadeSmartwall, ocorrencias: ocorrenciasPorEquipe.get(codigo) || [] });
+            });
+        });
+
+        return base;
+    }
+
+    function nomeZona(regiao, index) {
+        if (normalizarZona(regiao) === "GERAL") return "GERAL";
+        const numero = String(regiao).match(/\d+/)?.[0] || String(index + 1);
+        return `ZONA ${numero}`;
+    }
+
     function atualizarListaAtivas(ativas) {
         const divLista = document.getElementById("listaAtivas");
         if (!divLista) return;
@@ -346,9 +470,10 @@ async function iniciarSmartwall() {
                 !kpiValoresAnteriores.encaminhadas.has(modalId);
             if (statusClass === "encaminhada") kpiValoresAnteriores.encaminhadas.add(modalId);
             const horaEnvio = formatarHoraComSegundos(ocorrencia.horaEnvio);
+            const zonaClass = getClasseZonaLista(ocorrencia);
 
             divLista.insertAdjacentHTML("beforeend", `
-                <button type="button" class="list-item ${statusClass} ${alertaEncaminhada ? "list-alert" : ""}" data-modal-id="${escapeHtml(modalId)}">
+                <button type="button" class="list-item ${statusClass} ${zonaClass} ${alertaEncaminhada ? "list-alert" : ""}" data-modal-id="${escapeHtml(modalId)}">
                     <span class="status-dot" aria-hidden="true"></span>
                     <span class="item-hora">${escapeHtml(horaEnvio)}</span>
                     <span class="item-tipo">${escapeHtml(ocorrencia.ocorrencia || "Sem natureza")}</span>
@@ -362,7 +487,9 @@ async function iniciarSmartwall() {
     function abrirModalRegistro(ocorrencia) {
         const modal = document.getElementById("smartRegistroModal");
         const content = document.getElementById("smartModalContent");
+        const title = document.getElementById("smartModalTitle");
         if (!modal || !content || !ocorrencia) return;
+        if (title) title.textContent = "Detalhes da ocorrência";
 
         const campos = [
             ["Nº Registro", ocorrencia.numRegistro || ocorrencia.numeroRegistro || "-"],
@@ -392,6 +519,31 @@ async function iniciarSmartwall() {
         modal.setAttribute("aria-hidden", "false");
     }
 
+    function abrirModalMotivoIndisponibilidade(nome, motivo, codigo) {
+        const modal = document.getElementById("smartRegistroModal");
+        const content = document.getElementById("smartModalContent");
+        const title = document.getElementById("smartModalTitle");
+        if (!modal || !content) return;
+
+        if (title) title.textContent = "Motivo da indisponibilidade";
+        content.innerHTML = `
+            <div class="smart-modal-field smart-modal-field-wide">
+                <strong>Agente</strong>
+                <span>${escapeHtml(nome || "-")}</span>
+            </div>
+            <div class="smart-modal-field">
+                <strong>Equipe</strong>
+                <span>${escapeHtml(codigo || "-")}</span>
+            </div>
+            <div class="smart-modal-field smart-modal-field-wide">
+                <strong>Motivo</strong>
+                <span>${escapeHtml(motivo || "Motivo não informado")}</span>
+            </div>
+        `;
+        modal.classList.add("open");
+        modal.setAttribute("aria-hidden", "false");
+    }
+
     function fecharModalRegistro() {
         const modal = document.getElementById("smartRegistroModal");
         if (!modal) return;
@@ -400,6 +552,34 @@ async function iniciarSmartwall() {
     }
 
     document.addEventListener("click", (event) => {
+        const nomeIndisponivel = event.target.closest(".region-agent-name-reason");
+        if (nomeIndisponivel) {
+            abrirModalMotivoIndisponibilidade(
+                nomeIndisponivel.dataset.agentName,
+                nomeIndisponivel.dataset.reason,
+                nomeIndisponivel.dataset.codigo
+            );
+            return;
+        }
+
+        const filtroTurno = event.target.closest(".shift-filter-btn[data-shift]");
+        if (filtroTurno) {
+            turnoAtendimentoAtual = filtroTurno.dataset.shift;
+            renderizarFiltroTurnos();
+            const historicoTurno = filtrarPorTurnoAtendimento(ultimosDocsHistoricoAgentes);
+            const ativosAgentesTurno = filtrarPorTurnoAtendimento(ultimosDocsAtivosAgentes);
+            const ocorrenciasAtivasTurno = filtrarPorTurnoAtendimento(ultimasOcorrenciasAtivas);
+            const frotaAtivaPorRegiao = calcularFrotaPorRegiao(ativosAgentesTurno);
+            const frotaHistoricoPorRegiao = calcularFrotaPorRegiao(historicoTurno);
+            const frotaGraficoPorRegiao = somarFrotasPorRegiao(frotaHistoricoPorRegiao, frotaAtivaPorRegiao);
+            const contagemRegiaoAtivas = calcularContagemRegiao(ocorrenciasAtivasTurno);
+            ultimaContagemRegiaoAtivas = contagemRegiaoAtivas;
+            atualizarResumoAtendimento(ocorrenciasAtivasTurno.length);
+            atualizarListaAtivas(ocorrenciasAtivasTurno.filter((ocorrencia) => ocorrencia.data_filtro === dataFiltro));
+            atualizarGraficoRegioes(contagemRegiaoAtivas, calcularContagemRegiao(historicoTurno), frotaAtivaPorRegiao, frotaGraficoPorRegiao, calcularAgentesPorRegiao(ativosAgentesTurno, ocorrenciasAtivasTurno));
+            return;
+        }
+
         const item = event.target.closest(".list-item[data-modal-id]");
         if (item) {
             abrirModalRegistro(ocorrenciasModal.get(item.dataset.modalId));
@@ -415,29 +595,158 @@ async function iniciarSmartwall() {
         if (event.key === "Escape") fecharModalRegistro();
     });
 
-    function atualizarGraficos(dadosRegiaoAtivas, dadosRegiaoHoje, dadosTipo, frotaAtivaPorRegiao, frotaHistoricoPorRegiao) {
-        atualizarGraficoRegioes(dadosRegiaoAtivas, dadosRegiaoHoje, frotaAtivaPorRegiao, frotaHistoricoPorRegiao);
+    function atualizarGraficos(dadosRegiaoAtivas, dadosRegiaoHoje, dadosTipo, frotaAtivaPorRegiao, frotaHistoricoPorRegiao, agentesAtivosPorRegiao) {
+        atualizarGraficoRegioes(dadosRegiaoAtivas, dadosRegiaoHoje, frotaAtivaPorRegiao, frotaHistoricoPorRegiao, agentesAtivosPorRegiao);
         atualizarGraficoTipos(dadosTipo);
-    }
+            const canvas = document.getElementById("chartTipos");
+            if (!canvas) return;
+
+            const tiposOrdenados = Object.entries(dadosTipo)
+                .filter(([, qtd]) => qtd > 0)
+                .sort((a, b) => b[1] - a[1]);
+            const total = tiposOrdenados.reduce((acc, [, qtd]) => acc + qtd, 0);
+            renderizarResumoTipo(tiposOrdenados, total);
+
+            const ctxTipos = canvas.getContext("2d");
+            if (myChartTipos) myChartTipos.destroy();
+
+            const maiorTipo = tiposOrdenados.reduce((max, [, qtd]) => Math.max(max, qtd), 0);
+            ctxTipos.font = "800 11px Segoe UI, sans-serif";
+            const maiorNomeTipo = tiposOrdenados.reduce((max, [tipo]) => Math.max(max, ctxTipos.measureText(String(tipo).toUpperCase()).width), 0);
+            const larguraCanvasTipo = Math.max(320, canvas.clientWidth || canvas.width || 320);
+            const reservaRotuloTipo = Math.min(0.62, Math.max(0.28, (maiorNomeTipo + 72) / larguraCanvasTipo));
+            const escalaMaximaTipo = Math.max(1, Math.ceil(maiorTipo / (1 - reservaRotuloTipo)));
+            const labelBarrasPlugin = {
+                id: "labelBarrasSmartwall",
+                afterDatasetsDraw(chart) {
+                    const { ctx, chartArea, scales } = chart;
+                    const xScale = scales.x;
+                    const yScale = scales.y;
+                    const dataset = chart.data.datasets[0];
+                    const canvasRight = chart.width - 8;
+                    const outsideGap = 10;
+                    const insideGap = 14;
+                    const temaDia = document.body.dataset.theme === "day";
+                    const corDentro = temaDia ? "#000000" : "#ffffff";
+                    const corFora = temaDia ? "#06131f" : "#e8f4ff";
+                    const isLargeScreen = window.matchMedia("(min-width: 1518px)").matches;
+                    const tamanhoLabelBase = isLargeScreen ? 18 : 11;
+                    const tamanhoValorBase = isLargeScreen ? 24 : 16;
+
+                    const ajustarFonte = (texto, peso, tamanhoBase, larguraMaxima) => {
+                        let tamanho = tamanhoBase;
+                        do {
+                            ctx.font = `${peso} ${tamanho}px Segoe UI, sans-serif`;
+                    const ctxRegioes = canvas.getContext("2d");
+                            if (ctx.measureText(texto).width <= larguraMaxima || tamanho <= 6) break;
+                            tamanho -= 1;
+                        } while (tamanho > 6);
+                        return tamanho;
+                    };
+
+                    ctx.save();
+                    ctx.textBaseline = "middle";
+                    chart.data.labels.forEach((label, index) => {
+                        const valor = dataset.data[index] || 0;
+                        const textoLabel = String(label).toUpperCase();
+                        const textoValor = String(valor);
+                        const y = yScale.getPixelForValue(index);
+                        const xBarraInicio = xScale.getPixelForValue(0);
+                        const xBarraFim = xScale.getPixelForValue(valor);
+                        const larguraBarra = Math.max(0, xBarraFim - xBarraInicio);
+                        ctx.font = `800 ${tamanhoLabelBase}px Segoe UI, sans-serif`;
+                        const larguraLabel = ctx.measureText(textoLabel).width;
+                        ctx.font = `900 ${tamanhoValorBase}px Segoe UI, sans-serif`;
+                        const larguraValor = ctx.measureText(textoValor).width;
+                        const cabeDentro = larguraBarra >= Math.max(larguraLabel, larguraValor) + (insideGap * 2);
+
+                        if (cabeDentro) {
+                            const xCentro = Math.max(chartArea.left + insideGap, Math.min(xBarraFim - insideGap, (xBarraInicio + xBarraFim) / 2));
+                            ctx.textAlign = "center";
+                            ctx.font = `800 ${tamanhoLabelBase}px Segoe UI, sans-serif`;
+                            ctx.fillStyle = corDentro;
+                            ctx.fillText(textoLabel, xCentro, y - 8);
+
+                            ctx.font = `900 ${tamanhoValorBase}px Segoe UI, sans-serif`;
+                            ctx.fillStyle = corDentro;
+                            ctx.fillText(textoValor, xCentro, y + 12);
+                            return;
+                        }
 
-    function atualizarGraficoRegioes(dadosRegiaoAtivas, dadosRegiaoHoje, frotaAtivaPorRegiao, frotaHistoricoPorRegiao) {
-        const canvas = document.getElementById("chartRegioes");
-        if (!canvas) return;
+                        const xFora = Math.min(xBarraFim + outsideGap, canvasRight - 12);
+                        const larguraFora = Math.max(52, canvasRight - xFora);
+                        ctx.textAlign = "left";
+                        ajustarFonte(textoLabel, 800, tamanhoLabelBase, larguraFora);
+                        ctx.fillStyle = corFora;
+                        ctx.fillText(textoLabel, xFora, y - 8);
+
+                        ajustarFonte(textoValor, 900, tamanhoValorBase, larguraFora);
+                        ctx.fillStyle = corFora;
+                        ctx.fillText(textoValor, xFora, y + 12);
+                    });
+                    ctx.restore();
+                }
+            };
 
-        const labelsResumo = regioesBase;
+            myChartTipos = new Chart(ctxTipos, {
+                type: "bar",
+                data: {
+                    labels: tiposOrdenados.map((tipo) => tipo[0]),
+                    datasets: [{
+                        label: "Ocorrencias",
+                        data: tiposOrdenados.map((tipo) => tipo[1]),
+                        backgroundColor: tiposOrdenados.map((_, index) => coresTipo[index] || coresTipo[0]),
+                        borderColor: tiposOrdenados.map((_, index) => coresTipo[index] || coresTipo[0]),
+                        borderWidth: 1,
+                        borderRadius: 6,
+                        barThickness: Math.max(24, Math.min(42, 240 / Math.max(1, tiposOrdenados.length)))
+                    }]
+                },
+                options: {
+                    indexAxis: "y",
+                    responsive: true,
+                    maintainAspectRatio: false,
+                    animation: false,
+                    layout: {
+                        padding: { left: 0, right: 34, top: 12, bottom: 4 }
+                    },
+                    plugins: {
+                        legend: { display: false },
+                        tooltip: {
+                            callbacks: {
+                                label: (context) => `${context.raw} ocorrencias`
+                            }
+                        }
+                    },
+                    scales: {
+                        x: {
+                            beginAtZero: true,
+                            grid: { color: "rgba(103, 153, 210, 0.28)", borderDash: [5, 6] },
+                            suggestedMax: escalaMaximaTipo,
+                            ticks: { display: false },
+                            title: { display: false }
+                        },
+                        y: {
+                            grid: { display: false },
+                            ticks: { display: false }
+                        }
+                    }
+                },
+                plugins: [labelBarrasPlugin]
+            });
+
+        const labelsResumo = regioesPainel;
         const totaisPorRegiao = labelsResumo.map((regiao) => {
             const frota = frotaHistoricoPorRegiao[regiao] || { mt: 0, vt: 0 };
             return (frota.mt || 0) + (frota.vt || 0);
         });
         const temDadosRegiao = totaisPorRegiao.some((valor) => valor > 0);
         const datasetData = temDadosRegiao ? totaisPorRegiao : [1];
-        const datasetColors = temDadosRegiao ? coresRegiao : ["rgba(255, 255, 255, 0.16)"];
+        const datasetColors = temDadosRegiao ? coresPainelRegiao : ["rgba(255, 255, 255, 0.16)"];
         const totalGeral = totaisPorRegiao.reduce((acc, valor) => acc + valor, 0);
         const totalMt = labelsResumo.reduce((acc, regiao) => acc + (frotaHistoricoPorRegiao[regiao]?.mt || 0), 0);
-        const totalVt = labelsResumo.reduce((acc, regiao) => acc + (frotaHistoricoPorRegiao[regiao]?.vt || 0), 0);
-        const ctxRegioes = canvas.getContext("2d");
         if (myChartRegioes) myChartRegioes.destroy();
-        renderizarRankingGraficoRegiao(labelsResumo, frotaAtivaPorRegiao);
+        renderizarRankingGraficoRegiao(labelsResumo, frotaAtivaPorRegiao, agentesAtivosPorRegiao, dadosRegiaoAtivas);
 
         const labelsDonutRegiaoPlugin = {
             id: "labelsDonutRegiaoSmartwall",
@@ -491,12 +800,12 @@ async function iniciarSmartwall() {
                     const pos = arc.tooltipPosition();
                     ctx.shadowColor = "rgba(0, 0, 0, 0.45)";
                     ctx.shadowBlur = 4;
-                    ctx.fillStyle = "#ffffff";
+                    ctx.fillStyle = temaDia ? "#071126" : "#ffffff";
                     ctx.font = "950 13px Segoe UI, sans-serif";
                     ctx.fillText(`${percentual}%`, pos.x, pos.y - 18);
                     ctx.font = "900 11px Segoe UI, sans-serif";
                     ctx.fillText(`Total ${valor}`, pos.x, pos.y);
-                    ctx.filter = "brightness(0) invert(1)";
+                    ctx.filter = temaDia ? "brightness(0)" : "brightness(0) invert(1)";
                     if (vtIconImg.complete) ctx.drawImage(vtIconImg, pos.x - 34, pos.y + 10, 14, 14);
                     if (mtIconImg.complete) ctx.drawImage(mtIconImg, pos.x + 8, pos.y + 10, 14, 14);
                     ctx.filter = "none";
@@ -548,24 +857,78 @@ async function iniciarSmartwall() {
         });
     }
 
-    function renderizarRankingGraficoRegiao(labels, frotaPorRegiao) {
+    function renderizarFiltroTurnos() {
+        const filtro = document.getElementById("shiftFilter");
+        if (!filtro) return;
+
+        filtro.innerHTML = turnosAtendimento.map((turno) => `
+            <button type="button" class="shift-filter-btn ${turnoAtendimentoAtual === turno.key ? "active" : ""}" data-shift="${turno.key}" style="--shift-color: ${turno.color}">
+                <strong>${turno.label}</strong>
+            </button>
+        `).join("");
+    }
+
+    function renderizarRankingGraficoRegiao(labels, frotaPorRegiao, agentesPorRegiao, ocorrenciasPorRegiao = {}) {
         const legend = document.getElementById("regionRankingPanel");
         if (!legend) return;
         const linhas = labels.map((regiao, index) => {
             const frota = frotaPorRegiao[regiao] || { mt: 0, vt: 0 };
             const total = (frota.mt || 0) + (frota.vt || 0);
+            const agentes = agentesPorRegiao?.[regiao] || { mt: [], vt: [] };
             return {
                 regiao,
+                zona: nomeZona(regiao, index),
+                numero: index + 1,
                 total,
                 mt: frota.mt || 0,
                 vt: frota.vt || 0,
-                cor: coresRegiao[index] || "#2f86ff"
+                ocorrencias: ocorrenciasPorRegiao?.[regiao] || 0,
+                agentesMt: agentes.mt || [],
+                agentesVt: agentes.vt || [],
+                cor: coresPainelRegiao[index] || "#2f86ff"
             };
-        }).sort((a, b) => b.total - a.total || a.regiao.localeCompare(b.regiao, "pt-BR"));
+        });
         const totalGeral = linhas.reduce((acc, item) => acc + item.total, 0);
+        const totalMt = linhas.reduce((acc, item) => acc + item.mt, 0);
+        const totalVt = linhas.reduce((acc, item) => acc + item.vt, 0);
         linhas.forEach((item) => {
             item.percentual = totalGeral ? ((item.total / totalGeral) * 100).toFixed(1).replace(".", ",") : "0,0";
+            item.temEquipe = item.agentesMt.length > 0 || item.agentesVt.length > 0;
         });
+        const renderizarNomesEquipe = (equipes, icone, alt, cor) => {
+            if (!equipes?.length) return "";
+            return equipes.map((equipe) => {
+                const nomes = Array.isArray(equipe) ? equipe : equipe.nomes;
+                const ocorrencias = Array.isArray(equipe) ? [] : equipe.ocorrencias || [];
+                const disponivelSmartwall = Array.isArray(equipe) ? true : equipe.disponivelSmartwall !== false;
+                const motivoIndisponibilidadeSmartwall = Array.isArray(equipe) ? "" : equipe.motivoIndisponibilidadeSmartwall || "";
+                const codigoEquipe = Array.isArray(equipe) ? "" : equipe.codigo || "";
+                const nomesUnicos = [...new Set((nomes || []).filter(Boolean))];
+                if (!nomesUnicos.length) return "";
+                return `
+                <span class="region-agent-group ${ocorrencias.length ? "region-agent-group-active" : ""}" style="--agent-color: ${cor}">
+                    <img src="${icone}" alt="${alt}">
+                    <span>
+                        ${codigoEquipe ? `<span class="region-agent-code">${escapeHtml(codigoEquipe)}</span>` : ""}
+                        ${nomesUnicos.map((nome) => disponivelSmartwall
+                            ? `<span class="region-agent-name region-agent-name-available">${escapeHtml(nome)}</span>`
+                            : `<button type="button" class="region-agent-name region-agent-name-unavailable region-agent-name-reason" data-agent-name="${escapeHtml(nome)}" data-reason="${escapeHtml(motivoIndisponibilidadeSmartwall)}" data-codigo="${escapeHtml(codigoEquipe)}">${escapeHtml(nome)}</button>`
+                        ).join("")}
+                    </span>
+                </span>
+            `;
+            }).join("");
+        };
+        const renderizarFrotaEquipe = (tipo, total, nomes, icone, alt) => `
+            <div class="region-fleet-panel region-fleet-${tipo}">
+                <div class="region-fleet-count">
+                    <img src="${icone}" alt="${alt}">
+                    <span class="region-fleet-type">${tipo.toUpperCase()}</span>
+                    <strong>${total}</strong>
+                </div>
+                <span class="region-agents">${renderizarNomesEquipe(nomes, icone, alt)}</span>
+            </div>
+        `;
 
         legend.innerHTML = `
             <div class="region-ranking-title">
@@ -573,32 +936,41 @@ async function iniciarSmartwall() {
                 <img src="src/live_png.png" alt="Ao vivo">
             </div>
             <div class="region-ranking-head">
-                <span>Região</span>
-                <span>Total / %</span>
+                <span>ZONA</span>
+                <span>OCORR&Ecirc;NCIAS</span>
+                <span>VT / EQUIPE</span>
+                <span>MT / EQUIPE</span>
             </div>
             <div class="region-ranking-list">
-                ${linhas.map((item, index) => `
-                    <div class="region-ranking-row" style="--legend-color: ${item.cor}">
-                        <span class="region-rank-wrap"><strong class="region-rank">${index + 1}</strong></span>
-                        <span class="region-info">
-                            <strong class="region-name">${escapeHtml(item.regiao)}</strong>
-                            <span class="region-fleet-counts">
-                                <span><img src="src/mt_png.png" alt="Moto"> ${item.mt}</span>
-                                <i aria-hidden="true"></i>
-                                <span><img src="src/vt_png.png" alt="Viatura"> ${item.vt}</span>
-                            </span>
-                        </span>
-                        <span class="region-metric">
-                            <strong class="region-total">${item.total}</strong>
-                            <strong class="region-percent">${item.percentual}%</strong>
-                        </span>
+                ${linhas.map((item) => `
+                    <div class="region-ranking-row ${item.temEquipe ? "" : "region-ranking-row-empty"}" style="--legend-color: ${item.cor}">
+                        <div class="region-info">
+                            <div class="region-name-line">
+                                <span>ZONA</span>
+                                <strong class="region-name ${/^ZONA\s+GERAL$/i.test(item.zona) ? "region-name-general" : ""}">${escapeHtml(item.zona.replace(/^ZONA\s+/i, ""))}</strong>
+                            </div>
+                            <div class="region-occurrence-cell">
+                                <strong>${item.ocorrencias}</strong>
+                                <span>${item.ocorrencias === 1 ? "ativa" : "ativas"}</span>
+                            </div>
+                            <div class="region-fleet-panel region-fleet-vt">
+                                <span class="region-agents">${renderizarNomesEquipe(item.agentesVt, "src/vt_png.png", "Viatura", item.cor) || "<span class=\"region-empty-agent\">Sem viatura</span>"}</span>
+                            </div>
+                            <div class="region-name-line">
+                                    <strong class="region-name">${escapeHtml(item.zona)}</strong>
+                                    <span class="region-occurrence-count">${item.ocorrencias} ocorrências</span>
+                                </div>
+                            <div class="region-fleet-panel region-fleet-mt">
+                                <span class="region-agents">${renderizarNomesEquipe(item.agentesMt, "src/mt_png.png", "Moto", item.cor) || "<span class=\"region-empty-agent\">Sem operador</span>"}</span>
+                            </div>
+                        </div>
                     </div>
                 `).join("")}
             </div>
             <div class="region-ranking-total">
-                <span>Total geral</span>
-                <strong>${totalGeral}</strong>
-                <strong>${totalGeral ? "100%" : "0%"}</strong>
+                <span class="region-total-fleet region-total-vt"><img src="src/vt_png.png" alt="Viatura"> ${totalVt}</span>
+                <span class="region-total-label">Total geral</span>
+                <span class="region-total-fleet region-total-mt"><img src="src/mt_png.png" alt="Moto"> ${totalMt}</span>
             </div>
         `;
     }
@@ -616,144 +988,150 @@ async function iniciarSmartwall() {
         centro.innerHTML = "";
     }
 
-    function atualizarGraficoTipos(dadosTipo) {
-        const canvas = document.getElementById("chartTipos");
+    function atualizarGraficoRegioes(dadosRegiaoAtivas, dadosRegiaoHoje, frotaAtivaPorRegiao, frotaHistoricoPorRegiao, agentesAtivosPorRegiao) {
+        const canvas = document.getElementById("chartRegioes");
         if (!canvas) return;
+        window.__debugRegiao = { entrou: true, data: Date.now() };
+        console.log("smartwall: atualizarGraficoRegioes", {
+            dadosRegiaoAtivas: Object.keys(dadosRegiaoAtivas || {}).length,
+            dadosRegiaoHoje: Object.keys(dadosRegiaoHoje || {}).length,
+            frotaAtiva: Object.keys(frotaAtivaPorRegiao || {}).length,
+            frotaHistorico: Object.keys(frotaHistoricoPorRegiao || {}).length,
+            agentesAtivos: Object.keys(agentesAtivosPorRegiao || {}).length
+        });
 
-        const tiposOrdenados = Object.entries(dadosTipo)
-            .filter(([, qtd]) => qtd > 0)
-            .sort((a, b) => b[1] - a[1]);
-        const total = tiposOrdenados.reduce((acc, [, qtd]) => acc + qtd, 0);
-        renderizarResumoTipo(tiposOrdenados, total);
+        const labelsResumo = regioesPainel;
+        const totaisPorRegiao = labelsResumo.map((regiao) => {
+            const frota = frotaHistoricoPorRegiao[regiao] || { mt: 0, vt: 0 };
+            return (frota.mt || 0) + (frota.vt || 0);
+        });
+        const temDadosRegiao = totaisPorRegiao.some((valor) => valor > 0);
+        const datasetData = temDadosRegiao ? totaisPorRegiao : [1];
+        const datasetColors = temDadosRegiao ? coresPainelRegiao : ["rgba(255, 255, 255, 0.16)"];
+        const totalGeral = totaisPorRegiao.reduce((acc, valor) => acc + valor, 0);
+        const totalMt = labelsResumo.reduce((acc, regiao) => acc + (frotaHistoricoPorRegiao[regiao]?.mt || 0), 0);
+        const totalVt = labelsResumo.reduce((acc, regiao) => acc + (frotaHistoricoPorRegiao[regiao]?.vt || 0), 0);
+        const ctxRegioes = canvas.getContext("2d");
+        if (myChartRegioes) myChartRegioes.destroy();
+        renderizarRankingGraficoRegiao(labelsResumo, frotaAtivaPorRegiao, agentesAtivosPorRegiao, dadosRegiaoAtivas);
 
-        const ctxTipos = canvas.getContext("2d");
-        if (myChartTipos) myChartTipos.destroy();
-
-        const maiorTipo = tiposOrdenados.reduce((max, [, qtd]) => Math.max(max, qtd), 0);
-        ctxTipos.font = "800 11px Segoe UI, sans-serif";
-        const maiorNomeTipo = tiposOrdenados.reduce((max, [tipo]) => Math.max(max, ctxTipos.measureText(String(tipo).toUpperCase()).width), 0);
-        const larguraCanvasTipo = Math.max(320, canvas.clientWidth || canvas.width || 320);
-        const reservaRotuloTipo = Math.min(0.62, Math.max(0.28, (maiorNomeTipo + 72) / larguraCanvasTipo));
-        const escalaMaximaTipo = Math.max(1, Math.ceil(maiorTipo / (1 - reservaRotuloTipo)));
-        const labelBarrasPlugin = {
-            id: "labelBarrasSmartwall",
-            afterDatasetsDraw(chart) {
-                const { ctx, chartArea, scales } = chart;
-                const xScale = scales.x;
-                const yScale = scales.y;
-                const dataset = chart.data.datasets[0];
-                const canvasRight = chart.width - 8;
-                const outsideGap = 10;
-                const insideGap = 14;
+        const labelsDonutRegiaoPlugin = {
+            id: "labelsDonutRegiaoSmartwall",
+            afterDraw(chart) {
+                const meta = chart.getDatasetMeta(0);
+                if (!meta?.data?.length) return;
+                const { ctx } = chart;
+                const centerX = meta.data[0].x;
+                const centerY = meta.data[0].y;
                 const temaDia = document.body.dataset.theme === "day";
-                const corDentro = temaDia ? "#000000" : "#ffffff";
-                const corFora = temaDia ? "#06131f" : "#e8f4ff";
-                const isLargeScreen = window.matchMedia("(min-width: 1518px)").matches;
-                const tamanhoLabelBase = isLargeScreen ? 18 : 11;
-                const tamanhoValorBase = isLargeScreen ? 24 : 16;
-
-                const ajustarFonte = (texto, peso, tamanhoBase, larguraMaxima) => {
-                    let tamanho = tamanhoBase;
-                    do {
-                        ctx.font = `${peso} ${tamanho}px Segoe UI, sans-serif`;
-                        if (ctx.measureText(texto).width <= larguraMaxima || tamanho <= 6) break;
-                        tamanho -= 1;
-                    } while (tamanho > 6);
-                    return tamanho;
-                };
-
+                const corTexto = temaDia ? "#ffffff" : "#142535";
+                const corSecundaria = corTexto;
                 ctx.save();
+                ctx.textAlign = "center";
                 ctx.textBaseline = "middle";
-                chart.data.labels.forEach((label, index) => {
-                    const valor = dataset.data[index] || 0;
-                    const textoLabel = String(label).toUpperCase();
-                    const textoValor = String(valor);
-                    const y = yScale.getPixelForValue(index);
-                    const xBarraInicio = xScale.getPixelForValue(0);
-                    const xBarraFim = xScale.getPixelForValue(valor);
-                    const larguraBarra = Math.max(0, xBarraFim - xBarraInicio);
-                    ctx.font = `800 ${tamanhoLabelBase}px Segoe UI, sans-serif`;
-                    const larguraLabel = ctx.measureText(textoLabel).width;
-                    ctx.font = `900 ${tamanhoValorBase}px Segoe UI, sans-serif`;
-                    const larguraValor = ctx.measureText(textoValor).width;
-                    const cabeDentro = larguraBarra >= Math.max(larguraLabel, larguraValor) + (insideGap * 2);
-
-                    if (cabeDentro) {
-                        const xCentro = Math.max(chartArea.left + insideGap, Math.min(xBarraFim - insideGap, (xBarraInicio + xBarraFim) / 2));
-                        ctx.textAlign = "center";
-                        ctx.font = `800 ${tamanhoLabelBase}px Segoe UI, sans-serif`;
-                        ctx.fillStyle = corDentro;
-                        ctx.fillText(textoLabel, xCentro, y - 8);
-
-                        ctx.font = `900 ${tamanhoValorBase}px Segoe UI, sans-serif`;
-                        ctx.fillStyle = corDentro;
-                        ctx.fillText(textoValor, xCentro, y + 12);
-                        return;
-                    }
+                ctx.shadowColor = "transparent";
+                ctx.shadowBlur = 0;
+                ctx.fillStyle = corTexto;
+                ctx.font = "900 10px Segoe UI, sans-serif";
+                ctx.fillText("TOTAL GERAL", centerX, centerY - 48);
 
-                    const xFora = Math.min(xBarraFim + outsideGap, canvasRight - 12);
-                    const larguraFora = Math.max(52, canvasRight - xFora);
-                    ctx.textAlign = "left";
-                    ajustarFonte(textoLabel, 800, tamanhoLabelBase, larguraFora);
-                    ctx.fillStyle = corFora;
-                    ctx.fillText(textoLabel, xFora, y - 8);
+                const iconY = centerY - 28;
+                const iconSize = 24;
+                ctx.filter = temaDia ? "brightness(0) invert(1)" : "brightness(0)";
+                if (mtIconImg.complete) ctx.drawImage(mtIconImg, centerX - 54, iconY - 10, iconSize, iconSize);
+                if (vtIconImg.complete) ctx.drawImage(vtIconImg, centerX + 30, iconY - 10, iconSize, iconSize);
+                ctx.filter = "none";
+                ctx.font = "950 24px Segoe UI, sans-serif";
+                ctx.fillStyle = corTexto;
+                ctx.fillText(String(totalMt), centerX - 42, centerY + 4);
+                ctx.fillText(String(totalVt), centerX + 42, centerY + 4);
+                ctx.strokeStyle = temaDia ? "rgba(15, 31, 54, 0.15)" : "rgba(255, 255, 255, 0.18)";
+                ctx.lineWidth = 1;
+                ctx.beginPath();
+                ctx.moveTo(centerX - 58, centerY + 25);
+                ctx.lineTo(centerX + 58, centerY + 25);
+                ctx.stroke();
+                ctx.fillStyle = corSecundaria;
+                ctx.font = "900 10px Segoe UI, sans-serif";
+                ctx.fillText("TOTAL", centerX, centerY + 41);
+                ctx.fillStyle = corTexto;
+                ctx.font = "950 24px Segoe UI, sans-serif";
+                ctx.fillText(String(totalGeral), centerX, centerY + 63);
 
-                    ajustarFonte(textoValor, 900, tamanhoValorBase, larguraFora);
-                    ctx.fillStyle = corFora;
-                    ctx.fillText(textoValor, xFora, y + 12);
+                meta.data.forEach((arc, index) => {
+                    const valor = chart.data.datasets[0].data[index] || 0;
+                    if (!valor) return;
+                    const regiao = chart.data.labels[index];
+                    const frota = frotaHistoricoPorRegiao[regiao] || { mt: 0, vt: 0 };
+                    const percentual = totalGeral ? ((valor / totalGeral) * 100).toFixed(1).replace(".", ",") : "0,0";
+                    const pos = arc.tooltipPosition();
+                    ctx.shadowColor = "rgba(0, 0, 0, 0.45)";
+                    ctx.shadowBlur = 4;
+                    ctx.fillStyle = temaDia ? "#071126" : "#ffffff";
+                    ctx.font = "950 13px Segoe UI, sans-serif";
+                    ctx.fillText(`${percentual}%`, pos.x, pos.y - 18);
+                    ctx.font = "900 11px Segoe UI, sans-serif";
+                    ctx.fillText(`Total ${valor}`, pos.x, pos.y);
+                    ctx.filter = temaDia ? "brightness(0)" : "brightness(0) invert(1)";
+                    if (vtIconImg.complete) ctx.drawImage(vtIconImg, pos.x - 34, pos.y + 10, 14, 14);
+                    if (mtIconImg.complete) ctx.drawImage(mtIconImg, pos.x + 8, pos.y + 10, 14, 14);
+                    ctx.filter = "none";
+                    ctx.font = "950 11px Segoe UI, sans-serif";
+                    ctx.fillText(String(frota.vt || 0), pos.x - 12, pos.y + 18);
+                    ctx.fillText(String(frota.mt || 0), pos.x + 30, pos.y + 18);
                 });
                 ctx.restore();
             }
         };
 
-        myChartTipos = new Chart(ctxTipos, {
-            type: "bar",
+        myChartRegioes = new Chart(ctxRegioes, {
+            type: "doughnut",
             data: {
-                labels: tiposOrdenados.map((tipo) => tipo[0]),
+                labels: labelsResumo,
                 datasets: [{
-                    label: "Ocorrencias",
-                    data: tiposOrdenados.map((tipo) => tipo[1]),
-                    backgroundColor: tiposOrdenados.map((_, index) => coresTipo[index] || coresTipo[0]),
-                    borderColor: tiposOrdenados.map((_, index) => coresTipo[index] || coresTipo[0]),
-                    borderWidth: 1,
-                    borderRadius: 6,
-                    barThickness: Math.max(24, Math.min(42, 240 / Math.max(1, tiposOrdenados.length)))
+                    data: datasetData,
+                    backgroundColor: datasetColors,
+                    borderColor: document.body.dataset.theme === "day" ? "#ffffff" : "rgba(255, 255, 255, 0.82)",
+                    borderWidth: 2,
+                    hoverOffset: 5,
+                    spacing: 2
                 }]
             },
             options: {
-                indexAxis: "y",
                 responsive: true,
                 maintainAspectRatio: false,
                 animation: false,
+                cutout: "46%",
                 layout: {
-                    padding: { left: 0, right: 34, top: 12, bottom: 4 }
+                    padding: { left: 18, right: 18, top: 18, bottom: 18 }
                 },
                 plugins: {
                     legend: { display: false },
                     tooltip: {
                         callbacks: {
-                            label: (context) => `${context.raw} ocorrencias`
+                            label: (context) => {
+                                const valor = context.raw || 0;
+                                const regiao = context.label;
+                                const frota = frotaHistoricoPorRegiao[regiao] || { mt: 0, vt: 0 };
+                                const percentual = totalGeral ? ((valor / totalGeral) * 100).toFixed(1).replace(".", ",") : "0,0";
+                                return `${regiao}: ${valor} (${percentual}%) | Moto ${frota.mt || 0} | Viatura ${frota.vt || 0}`;
+                            }
                         }
                     }
-                },
-                scales: {
-                    x: {
-                        beginAtZero: true,
-                        grid: { color: "rgba(103, 153, 210, 0.28)", borderDash: [5, 6] },
-                        suggestedMax: escalaMaximaTipo,
-                        ticks: { display: false },
-                        title: { display: false }
-                    },
-                    y: {
-                        grid: { display: false },
-                        ticks: { display: false }
-                    }
                 }
             },
-            plugins: [labelBarrasPlugin]
+            plugins: [labelsDonutRegiaoPlugin]
         });
     }
 
+    function atualizarGraficoTipos(dadosTipo) {
+        const tiposOrdenados = Object.entries(dadosTipo)
+            .filter(([, qtd]) => qtd > 0)
+            .sort((a, b) => b[1] - a[1]);
+        const total = tiposOrdenados.reduce((acc, [, qtd]) => acc + qtd, 0);
+        renderizarResumoTipo(tiposOrdenados, total);
+    }
+
     function renderizarResumoTipo(tiposOrdenados, total) {
         const legend = document.getElementById("typeLegend");
         const totalEl = document.getElementById("typeTotalHoje");
@@ -761,6 +1139,7 @@ async function iniciarSmartwall() {
         if (!legend) return;
 
         legend.innerHTML = "";
+
         tiposOrdenados.forEach(([tipo, qtd], index) => {
             const cor = coresTipo[index] || coresTipo[0];
             legend.insertAdjacentHTML("beforeend", `
diff --git a/smartwall.html b/smartwall.html
index 4b86ad6..190f933 100644
--- a/smartwall.html
+++ b/smartwall.html
@@ -7,7 +7,7 @@
     
     <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
     <link rel="stylesheet" href="css/theme-switch.css">
-    <link rel="stylesheet" href="css/smartwall.css?v=20260630-9">
+    <link rel="stylesheet" href="css/smartwall.css?v=20260703-66">
     <link rel="stylesheet" href="css/app-menu.css?v=20260626-1">
 </head>
 <body>
@@ -80,9 +80,8 @@
                     <img src="src/total_png.png" alt="">
                 </div>
                 <div>
-                    <h3>Total (Hoje)</h3>
                     <div class="value value-blue" id="kpiTotal">0</div>
-                    <p>Ocorrências registradas</p>
+                    <h3>Ocorrências registradas</h3>
                 </div>
             </div>
             <div class="kpi-card border-red">
@@ -90,9 +89,8 @@
                     <img src="src/despacho_png.png" alt="">
                 </div>
                 <div>
-                    <h3>Para Despacho</h3>
                     <div class="value value-red" id="kpiDespacho">0</div>
-                    <p>Ocorrências para despacho</p>
+                    <h3>Ocorrências para despacho</h3>
                 </div>
             </div>
             <div class="kpi-card border-orange">
@@ -100,9 +98,8 @@
                     <img src="src/andamento_png.png" alt="">
                 </div>
                 <div>
-                    <h3>Em Andamento</h3>
                     <div class="value value-orange" id="kpiAndamento">0</div>
-                    <p>Ocorrências ativas</p>
+                    <h3>Ocorrências ativas</h3>
                 </div>
             </div>
             <div class="kpi-card border-green">
@@ -110,9 +107,8 @@
                     <img src="src/concluido_png.png" alt="">
                 </div>
                 <div>
-                    <h3>Concluídas (Hoje)</h3>
                     <div class="value value-green" id="kpiConcluidas">0</div>
-                    <p>Ocorrências finalizadas</p>
+                    <h3>Ocorrências finalizadas</h3>
                 </div>
             </div>
             <div class="kpi-card border-red" id="kpiVtrsCard">
@@ -120,9 +116,8 @@
                     <img src="src/veiculos_png.png" alt="">
                 </div>
                 <div>
-                    <h3>Veículos em Campo</h3>
                     <div class="value value-red" id="kpiVtrs">0</div>
-                    <p>Viaturas em atendimento</p>
+                    <h3>Viaturas em atendimento</h3>
                 </div>
             </div>
         </section>
@@ -133,12 +128,13 @@
                     <div class="panel-heading">
                         <h2 class="panel-title">
                             <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M21 12A9 9 0 1 1 12 3v9z"></path><path d="M12 3a9 9 0 0 1 9 9h-9z"></path></svg>
-                            Histórico total de MT / VT por região
+                            Histórico total de MT / VT por zona
                         </h2>
                     </div>
                     <div class="canvas-container region-chart-wrap">
                         <canvas id="chartRegioes"></canvas>
                     </div>
+                    <div class="shift-filter" id="shiftFilter" aria-label="Filtrar viaturas por turno"></div>
                 </div>
                 <div class="region-ranking-panel" id="regionRankingPanel"></div>
             </section>
@@ -180,7 +176,7 @@
 
     <script src="js/theme-switch.js"></script>
     <script src="js/app-menu.js?v=20260701-3"></script>
-    <script src="js/smartwall.js?v=20260630-1"></script>
+    <script src="js/smartwall.js?v=20260703-20"></script>
     <div class="smart-modal" id="smartRegistroModal" aria-hidden="true">
         <div class="smart-modal-backdrop" data-close-smart-modal></div>
         <section class="smart-modal-card" role="dialog" aria-modal="true" aria-labelledby="smartModalTitle">
